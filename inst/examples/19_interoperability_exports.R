library(gamlssPosthoc)
# After creating `res <- gamlss_posthoc(...)`:
# generics::tidy(res)
# generics::glance(res)
# generics::augment(res)
# if (requireNamespace("modelsummary", quietly=TRUE)) modelsummary::modelsummary(list(Posthoc=res))
# if (requireNamespace("parameters", quietly=TRUE)) parameters::model_parameters(res)
# if (requireNamespace("flextable", quietly=TRUE)) as_flextable(res)
