% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlss_distribution_summary.R
\name{gamlss_distribution_summary}
\alias{gamlss_distribution_summary}
\title{Summarize full predicted distributions from GAMLSS models}
\usage{
gamlss_distribution_summary(
  object,
  newdata,
  probs = c(0.025, 0.5, 0.975),
  data = NULL,
  positive_dist_fun = NULL
)
}
\arguments{
\item{object}{A fitted \code{gamlss} or \code{gamlssZadj} model.}

\item{newdata}{Data frame containing predictor values at which to predict.}

\item{probs}{Numeric probabilities in \verb{[0, 1]} for requested quantiles.}

\item{data}{Optional original model data.}

\item{positive_dist_fun}{Optional constructor for a positive-part
\code{distributions3} object when the family cannot be reconstructed generically.}
}
\value{
A data frame containing \code{newdata}, mean, variance, standard deviation,
exact probability mass at zero, and requested quantiles.
}
\description{
Returns plug-in summaries of ordinary \code{gamlss} distributions and generic
zero-adjusted \code{gamlssZadj} distributions. For a zero-adjusted model, the
positive-part distribution is reconstructed through \code{gamlss.dist::GAMLSS()}
whenever possible and combined with the predicted zero probability.
}
\examples{
if (requireNamespace("gamlss", quietly = TRUE) &&
    requireNamespace("gamlss.dist", quietly = TRUE) &&
    requireNamespace("distributions3", quietly = TRUE)) {
  set.seed(12)
  d <- data.frame(x = seq(0.2, 2, length.out = 60))
  d$y <- gamlss.dist::rGA(nrow(d), mu = exp(0.1 + 0.35 * d$x), sigma = 0.28)
  fit <- gamlss::gamlss(y ~ x, data = d, family = gamlss.dist::GA, trace = FALSE)
  gamlss_distribution_summary(fit, data.frame(x = c(0.5, 1, 1.5)), data = d)
}
}
