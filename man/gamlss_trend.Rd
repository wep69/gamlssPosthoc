% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlss_trend.R
\name{gamlss_trend}
\alias{gamlss_trend}
\title{Quantitative trends, derivatives, turning points, and optima from GAMLSS}
\usage{
gamlss_trend(
  object,
  x,
  by = NULL,
  at_x = NULL,
  n = 100L,
  method = c("curve", "derivative", "turning_points", "optimum"),
  derivative_order = 1L,
  optimum = c("maximum", "minimum"),
  band = c("pointwise", "simultaneous"),
  estimand = "mean",
  what = "mu",
  engine = c("auto", "marginaleffects", "distribution"),
  population = NULL,
  weights = NULL,
  grid = NULL,
  data = NULL,
  uncertainty = c("auto", "delta", "simulation", "bootstrap", "none"),
  bootstrap = "case",
  B = 499L,
  cluster = NULL,
  custom_fun = NULL,
  positive_dist_fun = NULL,
  refit_fun = NULL,
  simulate_fun = NULL,
  parallel = FALSE,
  cores = 2L,
  seed = 1234
)
}
\arguments{
\item{object}{Fitted GAMLSS model.}

\item{x}{Name of a numeric predictor.}

\item{by}{Optional grouping variables.}

\item{at_x}{Numeric evaluation grid. If \code{NULL}, an equally spaced grid is used.}

\item{n}{Number of grid points when \code{at_x} is \code{NULL}.}

\item{method}{\code{curve}, \code{derivative}, \code{turning_points}, or \code{optimum}.}

\item{derivative_order}{Derivative order, currently 1 or 2.}

\item{optimum}{For \code{method = "optimum"}, select \code{maximum} or \code{minimum}.}

\item{band}{\code{pointwise} or \code{simultaneous}. Simultaneous bands require
bootstrap draws.}

\item{estimand}{Distributional estimand passed to \code{\link[=gamlss_posthoc]{gamlss_posthoc()}}.}

\item{what}{Distributional parameter when \code{estimand = "parameter"}.}

\item{engine}{\code{auto}, \code{marginaleffects}, or \code{distribution}. First derivatives
of ordinary distributional parameters can use \code{marginaleffects::avg_slopes()}.}

\item{population}{Target standardization population.}

\item{weights}{Weighting rule passed to \code{\link[=gamlss_posthoc]{gamlss_posthoc()}}.}

\item{grid}{Deprecated alias for \code{population}.}

\item{data}{Original model data.}

\item{uncertainty}{\code{auto}, \code{delta}, \code{simulation}, \code{bootstrap}, or \code{none}.
Delta/simulation are available when \code{marginaleffects} owns a first-derivative
parameter trend; distribution-derived curves use refit bootstrap when these
covariance-based layers are requested. Derivative bands are based on retained
bootstrap curves.}

\item{bootstrap}{Bootstrap type.}

\item{B}{Number of bootstrap refits.}

\item{cluster}{Cluster column for cluster bootstrap.}

\item{custom_fun}{Optional custom estimand function.}

\item{positive_dist_fun}{Optional positive-distribution constructor for
zero-adjusted models.}

\item{refit_fun}{Optional custom model-refit function.}

\item{simulate_fun}{Optional custom response simulator.}

\item{parallel}{Use parallel bootstrap through \code{future.apply}.}

\item{cores}{Number of workers; values above two are reduced to two.}

\item{seed}{Random seed.}
}
\value{
Object of class \code{gamlss_trend} with curve/derivative values and,
where requested, detected turning points or extrema.
}
\description{
Evaluates a declared estimand along a numeric predictor. The function can
return the fitted curve, first or second numerical derivatives, turning
points, or extrema locally refined around an interior grid optimum. Prediction-based derivatives remain available
for GAMLSS smoothers such as \code{pb()}.
}
